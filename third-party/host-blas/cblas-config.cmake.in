if(NOT TARGET cblas@_SUFFIX64@)
  find_package(OpenBLAS@_SUFFIX64@ REQUIRED)
  add_library(cblas@_SUFFIX64@ INTERFACE)
  target_link_libraries(cblas@_SUFFIX64@ INTERFACE OpenBLAS@_SUFFIX64@::OpenBLAS)
  set(CBLAS_LIBRARIES cblas@_SUFFIX64@)
endif()
